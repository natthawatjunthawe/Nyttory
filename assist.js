window.initAssist=(dbP,dbA,rF,u,c,emj)=>{setInterval(async()=>{if(!navigator.onLine||!u.length||!c.length)return;const p=await dbA("ps");if(!p||!p.length||Math.random()>.4)return;let pt=p[Math.floor(Math.random()*p.length)],b=u[Math.floor(Math.random()*u.length)];if(!pt.bMem)pt.bMem={};if(!pt.bMem[b.id])pt.bMem[b.id]=[];if(Math.random()>.6){const k=Object.keys(emj)[Math.floor(Math.random()*6)];pt.reactions[k]++;await dbP("ps",pt);rF()}else{const ac=c.filter(x=>!pt.bMem[b.id].includes(x));if(!ac.length)return;const txt=ac[Math.floor(Math.random()*ac.length)];pt.bMem[b.id].push(txt);const d=document.getElementById('bTyp');if(d){document.getElementById('bTypT').innerText=`${b.name} is typing...`;d.style.display="flex";setTimeout(async()=>{d.style.display="none";pt.comments.push({id:"c_"+Date.now(),authorId:b.id,authorName:b.name,authorAvatar:b.avatar,text:txt,ts:Date.now()});await dbP("ps",pt);rF();if(window.aud)window.aud.pop.play()},Math.random()*8000+3000)}}},Math.random()*15000+10000)};